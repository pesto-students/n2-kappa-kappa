import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  table: {
    minWidth: 650,
  },
  actionButton: {
    padding: 'unset',
  },
  root: {
    maxHeight: 480,
  },
}));
